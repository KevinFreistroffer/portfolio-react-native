import { DefaultTheme } from 'react-native-paper';

export const Theme = {
  ...DefaultTheme,
  font: {
    fontFamily: 'Lato_400Regular'
  },
  colors: {
    ...DefaultTheme.colors
  }
};

export default Theme;
